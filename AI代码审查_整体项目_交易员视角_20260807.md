# PR Review Template

## 审查基本信息

| 项目 | 内容 |
| --- | --- |
| 评审时间 | 2026-08-07 |
| 开发人员 | xiaoyao2495（本地 git 配置；以 git log 为准） |
| 比对分支 | 整体项目审计：工作区（含未提交 5m 机会扫描器）vs HEAD(30f1735) |
| 审查工程 | daily_bias（ICT 2022 4H Daily Bias Engine + 4H Bias Monitor + 5m 机会发现） |
| 使用技能 | code-review-skill-enhance |
| 需求编码 | 整体项目审计（无关联 PR/需求；含上一轮新增的 5m 机会扫描器，未提交） |
| 变更文件数 | 4（2 修改 + 2 新增） |
| 代码行数 | +162 / -1（已跟踪 diff）；新增 monitor/opportunity.js、test/opportunity.test.js |
| 审查结论 | Comment（有条件通过）：核心引擎与监控链路质量高、测试完备；但未提交的 5m 机会扫描器存在决策层脱节与升级兼容两处重要风险，建议修复后合入；另有一处 4H 结构失效判定存在插针翻转刷屏风险 |
| 是否高风险 | 否（无阻塞级问题；重要级 4 项均可在合入前修复） |

## 变更概览

项目整体为「4H 环境监控 + 5m 机会发现」两段式：核心引擎（engine/ + indicators/）输出 Bias/Scenario/Confidence/Decision/Quality，监控层（monitor/ + scripts/runMonitor.js）定时扫描 Top12 合约、状态变化时推钉钉（⚠️ Bias 变化 / ℹ️ 更新 / ⚡ 扫损 / 📊 收盘报告 / 🎯 5m 机会 / 📋 机会榜）。

本次审计范围 = 全仓代码（engine / indicators / monitor / data / report / scanner / scripts / test）+ 上一轮未提交的 5m 机会扫描器。

- 核心引擎：Structure→Bias（只由 4H 结构决定）、Liquidity→Draw、Dealing Range→Location/Context、Scenario/Confidence/Decision 分层清晰，符合 ICT 2022 语义
- 监控链路：统一 analyzeBias 共用链路（实时/回放一致），数据层缓存+回退+代理策略完善，推送去重/失败重试机制健全
- 新增（未提交）：5m 机会扫描器（RETRACE 回踩 / BOS 突破 / CHAIN 扫损→MSS→回踩 三类信号 + 评分门槛 + 60 分钟冷却 + 30 分钟机会榜）

### 变更文件

```text
M  scripts/runMonitor.js          （P2 机会扫描集成 + 消息构建）
M  test/runMonitor.test.js        （机会消息格式测试）
A  monitor/opportunity.js         （新增：5m 机会扫描器，未跟踪）
A  test/opportunity.test.js       （新增：机会扫描器单测，未跟踪）
```

### 变更统计

```text
 scripts/runMonitor.js   | 122 ++++++++++++++++++++++++++++++++++++++++++++++++
 test/runMonitor.test.js |  41 +++++++++++++++++++++++-
 2 files changed, 162 insertions(+), 1 deletion(-)
 （另新增 monitor/opportunity.js ≈170 行、test/opportunity.test.js ≈110 行，未跟踪）
```

## 风险清单

### 🟡[重要] 4H 结构失效判定用 5m 实时价直判 → 插针可触发 Bias 快速翻转刷屏

- 提交人员：xiaoyao2495
- 需求编码：整体项目审计
- 文件位置：`engine/dailyBiasEngine.js`（L50-52）、`monitor/biasMonitor.js`（L62-63）
- 关联文件：`engine/confidence.js`（L125-128 已记录同类翻转）
- 风险维度：交易逻辑正确性 / 推送噪声

**问题描述**

`dailyBiasEngine.js` 的 `brokenLow = price <= lastLow.price` / `brokenHigh` 与 `validateProtectedStructure` 均使用 `biasMonitor` 传入的 `price`，而该值在实时模式下是 **5m 进行中 K 的最新成交价**（`price5m ?? price4h`）。即 4H 结构失效（INVALIDATED）由 5m 实时价直接判定，没有收盘确认或时间确认缓冲。

**风险影响**

- 价格插针瞬时跌破 4H lastLow → 本轮判 `INVALIDATED` → `effectiveBias=NEUTRAL` → 推送 ⚠️ Bias 变化；价格收回后下轮又恢复 VALID → 再次推送 ⚠️ 反向变化。10 分钟轮下可在短时间内来回翻转。
- 与 `mss.js` 自己声明的「wick 插针后收回不算结构转移，事件需收盘确认」语义不一致（5m 层有确认、4H 层无确认）。
- 项目已有先例：`confidence.js` L125-128 注释记录了「开关因子随微小价格波动在阈值 40 附近来回翻转，10 分钟刷屏（MEDIUM 45 ↔ LOW 20）」——同一类根因已在 4H 结构层出现。
- 交易影响：交易者被高频矛盾推送误导反复改判，是监控系统最大的信任杀手。

**建议修复代码**

```js
// biasMonitor.js：判定结构失效/confidence 用"确认价"，实时价仅用于展示
// 方案 A（最小改动）：5m 末根已收盘 → 用其 close（收盘确认）；
// 进行中 → 用"最近一根已收盘 5m 的 close"，避免实时插针
const last5m = m5.length ? m5[m5.length - 1] : null;
const lastClosed5m = last5m && last5m.closeTime <= Date.now()
  ? last5m
  : m5.length > 1 ? m5[m5.length - 2] : null;
const price = lastClosed5m ? lastClosed5m.close : (price5m ?? price4h);
// 方案 B（更强）：4H 结构失效增加"连续两轮确认"缓冲，或仅在 4H 收盘判定（牺牲时效）
```

---

### 🟡[重要] 机会评分未纳入 Decision 层：4H `NO_TRADE`（confidence LOW）时 CHAIN 信号仍可能越过推送门槛

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`monitor/opportunity.js`（scoreOpportunity，L160-172）、`scripts/runMonitor.js`（L422-426）
- 关联文件：`engine/decision.js`（L55-57 NO_TRADE 规则）
- 风险维度：信号一致性 / 交易误导

**问题描述**

`scoreOpportunity` 的环境分包含 confidence/quality/session/structureStatus 与信号类型权重，但**完全没有读取 `env.decision`**。当 4H confidence=LOW（score 0）且 decision=NO_TRADE 时：CHAIN(25) + quality HIGH(15) + session(10) + VALID(5) + zone(5) = 60，恰好达到 `OPP_MIN_SCORE=60` 推送门槛。即 4H 明确「方向概率过低、不交易」时，5m 机会层仍可能推送「顺该 4H bias 做多/做空」的机会。

**风险影响**

- 两层信号自相矛盾：环境层说 NO TRADE，机会层说「顺 bias 做多/做空」。用户（5m 日内交易者）以机会消息为准入场，实际是在 4H 方向概率最低时追单。
- 用户实测的 MUUSDT 当前正是 confidence LOW 20 / NO TRADE，若其 CHAIN 条件成立就会触发此矛盾推送。

**建议修复代码**

```js
// opportunity.js scoreOpportunity 起始处（决策层优先拦截）
if (env.decision === "NO_TRADE") return Math.min(score, OPP_MIN_SCORE - 1); // 或直接 return 0
// 更严格：决策层 WATCH 是"可找入场"的最低要求
// 并在机会消息环境行补充展示当前 4H 操作，让信号上下文自洽
```

---

### 🟡[重要] 机会消息把「观察位」表述为「入场参考」，缺少入场确认条件

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`scripts/runMonitor.js`（buildOpportunity L473）、`monitor/opportunity.js`（detectRetrace/detectChain）
- 风险维度：交易决策误导

**问题描述**

RETRACE 信号仅表示「价格正回踩同向 5m 执行区（±0.3% 贴边）」，CHAIN/BOS 也仅标注触发事件，**均未包含入场确认条件**（如触及执行区后出现 5m 反K / 收盘收回 / 二次结构确认）。但消息文案直接写「入场参考: 881.0」，易被理解为可直接市价入场的价位。

**风险影响**

- ICT 回踩入场需要"到达执行区 + 确认信号"，缺确认直接入场会追在回踩中段（价格可能继续下破执行区）。
- 用户是 5m 日内交易者，对这类文案的信任度决定实际下单行为，措辞必须诚实。

**建议修复代码**

```js
// buildOpportunity 文案区分"观察/挂单位"与"确认后入场"：
lines.push(`观察位: ${fmtPrice(op.entry)}（现价 ${fmtPrice(env.price)}，需回踩后确认）`);
// 或机会对象增加 confirmed 字段：RETRACE 一律未确认，CHAIN/BOS 标"事件已确认但入场需执行区确认"
```

---

### 🟡[重要] 升级兼容：旧 state.json 无 `oppPushed` 字段 → 升级后首轮机会扫描整段中断

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`scripts/runMonitor.js`（scanAndPushOpportunities L425）
- 风险维度：兼容性 / 数据链路

**问题描述**

服务器上现有 state.json 由旧版本写入，合约条目无 `oppPushed` 字段。`scanAndPushOpportunities` 中 `prevState[op.symbol].oppPushed[op.key]` 在 `oppPushed` 为 undefined 时抛 `TypeError`，且该段代码没有 per-op try/catch，会冒泡到外层 `try { await scanAndPushOpportunities(...) } catch` ——**整个机会扫描段（含所有合约的机会推送与机会榜）在升级后第一轮全部中断**，仅 log 一条"机会扫描失败"。第二轮起 `cur.oppPushed={}` 落地后才自愈。

**风险影响**

- 升级窗口内首轮机会全部漏报（若恰好出现强机会则漏）。
- 第一轮日志出现误导性错误堆栈，排查成本。

**建议修复代码**

```js
// runMonitor.js scanAndPushOpportunities
const lastTs = prevState[op.symbol]?.oppPushed?.[op.key] || 0;
// 顺带把 per-op 推送放进 try 内，单条失败不拖垮整段
```

---

### 🟢[建议] `opportunity.js` CLI 相关 import 位于文件中部

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`monitor/opportunity.js`（L135-137 `import pathToFileURL/getHistory` 在文件中间）
- 风险维度：代码规范

**问题描述**

CLI 专用的 `pathToFileURL`/`getHistory` import 写在文件中部（ESM hoisted 可运行），但违背「import 一律置于文件顶部」的惯例，影响可读性与静态分析。

**建议修复代码**

将两个 import 上移到文件顶部，与既有 import 合并。

---

### 🟢[建议] `detectRetrace` 的 `sideOk` 判定恒真（冗余）

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`monitor/opportunity.js`（detectRetrace）
- 风险维度：代码质量

**问题描述**

外层已用 `price < z.bottom - margin || price > z.top + margin` 过滤，区间内的 `price >= z.bottom - margin` 恒成立，`sideOk` 冗余。保留无害但增加阅读负担。

**建议修复代码**

删除 `sideOk` 判断，仅保留 `dist` 计算。

---

### 🟢[建议] 机会信号时效性：10 分钟轮 + 5m 缓存 TTL 5 分钟 → 信号延迟上限约 15 分钟

- 提交人员：xiaoyao2495
- 需求编码：新增 5m 机会扫描器（未提交）
- 文件位置：`scripts/runMonitor.js`（OPP_M5_LIMIT / 调度节拍）
- 风险维度：交易时效

**问题描述**

5m 交易者视角，从「价格开始回踩/突破」到收到推送最长约 15 分钟（对齐轮 ≤10min + 5m 缓存 ≤5min + 5m 收盘确认）。对 5m 入场偏慢，更接近"环境提醒"而非"入场信号"。

**建议修复代码**

维持现状（作为环境提醒是安全的）；若需提效，可对 opportunity 单独缩短 5m 缓存 TTL 或用实时价触发（需先解决插针误报问题，见重要 #1）。

---

### 🟢[建议] `planR` 风险分母用 `protectedLow/High`（深位），机会质量可能系统性偏低

- 提交人员：xiaoyao2495
- 需求编码：整体项目审计
- 文件位置：`engine/decision.js`（L36-37）、`engine/dailyBiasEngine.js`（buildInvalidation）
- 风险维度：评分口径

**问题描述**

`risk = |price − invalidation|`，invalidation 是结构保护位（最后一个 HH 之前的低点，深位）。实际交易者止损通常更贴近（如结构低点下方），planR 分母偏大 → planR 系统性偏保守 → Quality 低估机会。作为"环境空间"代理是保守偏安全的方向，但需知晓该口径。

**建议修复代码**

无需修改（保守口径符合 ICT 失效位语义）；建议在 Quality 定义注释中补充说明「planR 分母 = 结构保护位（深位），不代表建议止损宽度」，避免用户误读为可下单盈亏比。

---

## 正向观察

- 交易逻辑分层严谨：Bias 只由 4H Structure 决定，Location/Context 只影响执行，Scenario/Confidence/Decision 各自独立职责——符合 ICT 2022「方向与位置拆离」。
- 统一分析链路 `engine/analyzeBias.js` 消除实时/回放双实现，行为约定（time 截断、幂等）注释清晰。
- 收盘确认语义在多处贯彻：mss.js（swing 只用已收盘 K + close beyond）、sweep.js（刺破后收回）、displacement.js（三条件 + 证据字段）。
- 数据层（data/binance.js）缓存 + 失败回退旧缓存 + 代理策略显式可控，降级路径完整；5m/1h 拉取失败降级为 []，不阻断主分析。
- 状态机健壮：推送失败保留旧状态下轮重试（宁可重复不漏报）、失败合约保留旧 state 防 isNew 漏推、cleanupState 白名单清理。
- 关键历史 bug 均有注释沉淀（KORUUSDT 扫损误报、ETH 信心度振荡、pm2 pm_exec_path 入口、undici fetch+ProxyAgent bug）。
- 消息格式统一钉钉规范（「检测-」关键词自动补齐、标题含时间戳、字段分层），ICT 术语保留英文、交互信息中文，符合项目命名规范。
- 测试完备：187 用例全过（含边界：决策枚举、贴边容差、插针、K 线截断、机会三类信号）。

## 验证记录

已执行以下命令：

```bash
git diff --check HEAD              # 空白错误检查 → exit 0
node --test                        # 全量测试 → 187 pass, 0 fail
node -e "import('./monitor/opportunity.js')..."   # 新模块语法/加载检查 → OK
HTTPS_PROXY=http://127.0.0.1:7890 node scripts/runMonitor.js MUUSDT --once --dry  # 真实数据 dry-run → 无报错，无 ≥60 机会（MUUSDT 当前 confidence LOW 符合环境过滤预期）
```

验证结论：

- 静态检查与全量测试通过。
- 真实数据链路（拉取 Top → 4H/5m/日/周 → 引擎 → 机会扫描）在 dry-run 下无异常。
- 未执行：服务器端 pm2 实机验证（需部署后按项目记忆检查 monitor/log.txt 与钉钉消息）。

## 新增开关及业务影响

| 开关/字段 | 类型 | 涉及流程 | 业务影响 | 审查结论 |
| --- | --- | --- | --- | --- |
| `OPP_MIN_SCORE=60` | 常量（机会推送门槛） | 5m 机会推送 | 决定机会消息是否推送 | 合理，但未纳入 decision（见重要 #2） |
| `OPP_COOLDOWN_MS=60min` | 常量（机会冷却） | 机会去重 | 同一 key 60 分钟不重复推 | 合理 |
| `OPP_M5_LIMIT=1000` | 常量（5m 历史长度） | 机会扫描数据量 | 每轮多拉 1000 根 5m（缓存 TTL 5min，约每 2 轮一次网络） | 请求增量可接受 |
| `state.oppPushed` | 状态字段 | 机会去重 | 记录已推机会 key→时间戳 | 需兼容旧 state.json（见重要 #4） |
| `monitor/opportunityDigest.json` | 运行时文件 | 机会榜节流 | 记录机会榜上次推送时间 | 无风险 |

### 业务影响说明

- 新增 5m 机会发现链路，使监控从"环境状态播报"升级为"机会提醒"，是本次审计的核心业务增量。
- 每轮新增约 12 次 5m 长历史缓存命中/拉取与 12 次机会扫描计算，耗时与请求量可控，不影响主监控节奏。
- 其余未发现新增配置或关键业务字段。

## 新增弹窗 / toast 提示

| 场景 | 提示方式 | 触发条件 | 用户可见文案/内容 | 是否阻断 | 审查结果 |
| --- | --- | --- | --- | --- | --- |
| 5m 机会发现 | 钉钉 markdown（🎯） | score ≥ 60 且 key 冷却期外 | `🎯 {symbol} 5m 机会` + 方向/执行区/入场参考/环境/触发/价格 | 否 | 文案需将"入场参考"改为"观察位"（见重要 #3） |
| 机会榜 | 钉钉 markdown（📊） | 北京时间 0/30 分整点轮，距上次 ≥25min | `📊 5m 机会榜` + Top5 列表 | 否 | 无风险 |

### 提示类风险关注

- 新增两条消息类型均不会阻断主流程；推送失败降级为 log 并下轮重试。
- 需关注消息频率：机会推送 60 分钟冷却 + 机会榜 30 分钟，联合可能在某合约持续出现机会时产生叠加消息；建议观察上线后 24h 消息量。

## 项目规范符合性检查结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 数据源统一 Binance 永续（fapi） | ✅ | 全部经 data/binance.js，缓存文件名含 `_perp` |
| webhook 仅环境变量 DINGTALK_WEBHOOK | ✅ | dingTalk.js 无硬编码，自动 strip 反引号/空白 |
| 钉钉消息含「检测-」关键词 | ✅ | sendMarkdown 自动补齐 |
| 代理仅 HTTPS_PROXY/HTTP_PROXY 显式开启 | ✅ | binance.js / topVolume.js 均如此 |
| 命名规范（ICT 术语英文 + 交互中文） | ✅ | 消息/日志符合 |
| pm2 fork 模式 CLI 入口兼容 pm_exec_path | ✅ | runMonitor.js L576-577 |
| 关键修复注释沉淀 | ✅ | 各模块文件头均有决策记录 |
| 新增代码 import 置于顶部 | ❌ | opportunity.js CLI import 在文件中部（建议级） |

## 建议重点回归场景

| 序号 | 场景 | 前置条件 | 操作/触发点 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| 1 | 升级后首轮机会扫描 | 服务器旧 state.json（无 oppPushed） | 部署新代码跑第一轮 | 机会扫描正常执行、不抛错（先修重要 #4） | P0 |
| 2 | 4H 结构失效插针翻转 | 结构 VALID、价格贴近 lastLow | 5m 实时价瞬时插针跌破又收回 | 不触发 ⚠️ 翻转推送（先修重要 #1） | P0 |
| 3 | NO_TRADE 时机会推送 | 4H confidence LOW + decision NO_TRADE + CHAIN 成立 | 触发机会扫描 | 机会层与决策层一致，不推送矛盾信号（先修重要 #2） | P0 |
| 4 | 机会推送失败重试 | 钉钉 webhook 临时失效 | 构造推送异常 | 机会 key 不落地，下轮重推；不中断主监控 | P1 |
| 5 | 5m 长历史拉取失败 | getHistory 抛错 | 任一合约数据异常 | 该合约机会跳过，其余合约正常 | P1 |
| 6 | 4H 收盘边界轮 | 北京 00/04/08/12/16/20 | force4h 刷新 + 收盘报告 | 报告用最新已收盘 4H；机会扫描不受影响 | P1 |
| 7 | 机会榜节流 | 连续多轮存在机会 | 北京 30 分整点 | 仅推送一次（≥25min 间隔生效） | P2 |
| 8 | 首轮全览 + 机会扫描 | 删除 state.json 重启 | --once 非 dry | 只推全览，不推机会（设计预期） | P2 |

## Test Coverage

| 类型 | 当前情况 | 建议 |
| --- | --- | --- |
| 单元测试（indicators/engine） | 187 用例覆盖 swing/structure/mss/pdArray/liquidity/dealingRange/sweep/displacement/killzone/decision/engine/explanation/scanner/state | 充分 |
| 机会扫描器单测 | opportunity.test.js 覆盖 RETRACE/BOS/CHAIN/NEUTRAL 过滤/LOW 环境过滤/computeM5Context | 充分 |
| 消息构建测试 | runMonitor.test.js 覆盖 buildChanged/buildSweep/buildCloseReport/buildOverview/buildOpportunity/buildOpportunityDigest | 充分 |
| 监控集成层 | scanAndPushOpportunities（拉数→扫描→去重→推送→机会榜）无直接单测 | 建议补：mock getHistory + 构造环境，验证升级兼容（重要 #4）与 NO_TRADE 拦截（重要 #2） |

### 覆盖不足风险

1. `scanAndPushOpportunities` 集成路径（含 oppPushed 兼容、冷却、失败重试、机会榜节流）无直接测试，本次审计的 4 项重要风险中有 2 项位于该函数，建议补充集成测试后合入。
2. 4H 结构失效的"实时价翻转"场景无回归测试（重要 #1），建议在 engine.test.js 增加"5m 实时价插针后恢复"用例。

## 上线后巡检建议

| 巡检项 | 检查位置 | 正常信号 | 异常信号 | 巡检时机 | 回滚/升级触发 |
| --- | --- | --- | --- | --- | --- |
| 机会推送频率 | 钉钉消息 + monitor/log.txt | 每合约机会 ≤ 1 次/小时 + 机会榜 ≤ 1 次/30min | 单合约同方向消息 2+/小时 或持续刷屏 | 上线后 24h | 回滚 = git revert 机会扫描改动（或临时提高 OPP_MIN_SCORE） |
| 机会扫描报错 | monitor/log.txt "机会扫描失败" | 无该日志 | 出现（多为 oppPushed 兼容问题，先修重要 #4） | 每日 | 修复兼容代码后重启 |
| 4H Bias 翻转频率 | 钉钉 ⚠️ 消息 | 翻转 1 次/数小时以上 | 同一合约 10-30 分钟内来回翻转 | 上线后 48h | 评估是否启用重要 #1 修复 |
| state.json / opportunityDigest.json | monitor/ | 文件可写、无异常增长 | 文件损坏/缺失（loadState/loadDigestTs 已容错返回默认值） | 每周 | 无需处理（自动重建） |
| 5m 缓存命中率 | data/cache/*_5m_h1000_* | 每 2 轮命中 1 次 | 每轮都拉取（TTL 异常） | 每周 | 检查 TTL_OVERRIDE 配置 |
| 钉钉限额 | 机器人频率限制 | 无 errcode 310000/130101 | 出现限流错误码 | 上线后 24h | 提高冷却/降低机会榜频率 |

## 审查结论

**结论：Comment（有条件通过）**

核心引擎与既有监控链路质量高、测试完备、可观测性强，交易逻辑分层符合 ICT 2022 语义，可以保留。本次新增的 5m 机会扫描器方向正确、与 4H 环境耦合的框架合理，但存在 4 项重要级问题需在合入前处理：① 4H 结构失效用 5m 实时价直判的插针翻转风险；② 机会评分未纳入 Decision 层（NO_TRADE 时仍可能推顺 bias 机会）；③ "入场参考"文案缺少入场确认条件；④ 旧 state.json 无 oppPushed 的升级兼容问题（升级后首轮机会扫描中断）。

问题统计：

| 级别 | 数量 |
| --- | --- |
| blocking | 0 |
| important | 4 |
| suggestion/nit | 4 |

合入前必须处理：

1. 修复升级兼容：`prevState[op.symbol]?.oppPushed?.[op.key] || 0`（重要 #4，P0）。
2. 机会评分纳入 decision：`env.decision === "NO_TRADE"` 时拦截（重要 #2，P0）。
3. 评估 4H 结构失效改用收盘确认价（重要 #1，P0，涉及既有 4H 行为，需确认不改监控预期后再动）。
4. 机会消息"入场参考"改"观察位"并注明需确认（重要 #3，P1）。
